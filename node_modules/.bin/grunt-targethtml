#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-targethtml/bin/grunt-targethtml" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-targethtml/bin/grunt-targethtml" "$@"
  ret=$?
fi
exit $ret
